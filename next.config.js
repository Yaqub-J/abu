/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com'],
        // You can add more domains as needed:
        // domains: ['images.pexels.com', 'example.com', 'another-domain.com'],
      },
}

module.exports = nextConfig
